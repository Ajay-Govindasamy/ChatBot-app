import React from 'react';

const Landing = () => (
        <div style={{textAlign : 'center' }}>
            <h1>Welcome to GUVI !</h1>
        A Bot which solves all of your Queries !
        </div>
    )

export default Landing;